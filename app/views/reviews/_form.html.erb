<%= simple_form_for([@album, @album.reviews.build]) do |f| %>
	<%= f.input :rating %>
	<%= f.input :comment %>
	<%= f.button :submit %>
<% end %>

<script type="text/javascript">
	$('#rating-form').raty({
		path: '/assets',
		scoreName: 'review[rating]'
	});
</script>